# This file overrides the settings in osm-server-setup/default_config.py
# Have a look inside that file for stuff that can be customized.

DB_PASSWORD = "override-me"

MAPNIK_INSTANCES = ["upstream", "custom"]
MAPNIK_INSTANCES_PARAMS = {
  "custom": {
    "param_0": "extension=jpg",
  }
}
